## 0.1.1

Point homepage, repository, and issue tracker at the renamed GitHub repo
`itsezlife/cancel_token`.

## 0.1.0

Initial release on pub.dev.

- `CancelToken` for cancel signals, Future tracking, and child linking
- `CancelableScope` for host lifetimes (`run`, `cancel`, `dispose`)
- `CancelledException` when scoped work is cancelled before a value
